# Production MySQL Database
spring.datasource.url=jdbc:mysql://quantity-measurement-db.ct4864ag0jsb.ap-south-1.rds.amazonaws.com:3306/quantitydb
spring.datasource.username=admin
spring.datasource.password=Admin1234!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration for MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server port
server.port=5000

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri=${APP_URL}/login/oauth2/code/google

# Actuator
management.endpoints.web.exposure.include=health,info
```

---

